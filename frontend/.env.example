# Frontend env. For local dev you can leave this unset (Vite proxies /api to
# localhost:5000). In production (Vercel), set this to your deployed backend
# origin so API calls and the Socket.io connection reach Render.
VITE_API_URL=https://citemind-backend.onrender.com
